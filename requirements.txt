# Requirements for 2-D Elasticity PINN (JAX)
# Install with: pip install -r requirements.txt
#
# For GPU support (Linux / WSL2 with CUDA 12):
#   pip install "jax[cuda12]" -r requirements.txt
#
# For CPU only (Windows native):
#   pip install -r requirements.txt

jax>=0.10.0
jaxlib>=0.10.0
flax>=0.8.0
optax>=0.2.0
numpy>=1.24
matplotlib>=3.7
tqdm>=4.65
plotly>=5.22
